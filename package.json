{
  "name": "arctic server",
  "version": "1.0.0",
  "description": "The backend routing required for the Arctic-UI component.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nlewanow",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.14.0",
    "mongoose": "^4.6.0"
  },
  "dependencies": {
    "promise": "^7.1.1"
  }
}
